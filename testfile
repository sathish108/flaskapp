pipeline {
    agent any
    stages {
        stage ('pulling code from git hub') {
            steps {
                git credentialsId: 'github-server-credentials', url: 'https://github.com/sathish108/flaskapp'    
		        stash 'Source'
            }    
        }
        stage ('pulling code from git hub') {
            steps {
                echo "hi sathish"
            }           
        }
    }
}