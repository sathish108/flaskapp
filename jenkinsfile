pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello sathish'
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
            cat /var/lib/jenkins/workspace/flaskapp/Dockerfile
        }
    }
}